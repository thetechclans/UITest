import { Max100Directive } from './max100.directive';

describe('Max100Directive', () => {
  it('should create an instance', () => {
    const directive = new Max100Directive();
    expect(directive).toBeTruthy();
  });
});
